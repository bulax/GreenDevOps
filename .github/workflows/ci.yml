name: Minimalne CI plik tekstowy

on:
push:
branches: [ main ]

jobs:
echo-test:
runs-on: ubuntu-latest
steps:
- name: Checkout kodu
uses: actions/checkout@v3

  - name: Wypisz zawartosc katalogu
    run: ls -la

  - name: Komunikat koncowy
    run: echo "GitHub Actions dziala‚a poprawnie"